function s = v2s(v)
% Перетворення вектора в рядок
% (10 позицій для кожної координати)
% Вхідний параметр:
%   v - вектор
% Вихідний параметр:
%   s - строковое представлення вектора

s = [];
n = length(v);
for i = 1:n
    s = [s r2s(v(i))];
end